/** Default Express JSON body limit for most API routes. */
export const DEFAULT_JSON_BODY_LIMIT = '100kb'

/** Bulk import sends full dataset JSON in one request. */
export const BULK_IMPORT_JSON_BODY_LIMIT = '20mb'
