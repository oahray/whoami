export const ROUND_START_DELAY_MS = 3000

/** Minimum clues (after difficulty filter) for an entity to appear on an in-person card. */
export const IN_PERSON_CLUES_MIN = 3

/** Maximum clues returned per in-person card (after shuffle). */
export const IN_PERSON_CLUES_MAX = 10

